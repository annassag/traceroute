/// <reference types="core-js" />
import Task from './task';
declare abstract class ShellTask extends Task {
    protected abstract getExecutableName(): string;
    protected abstract getParameters(): string[];
    protected isExecutableAvailable(executableName: string): boolean;
    run(): Promise<string>;
}
export default ShellTask;
