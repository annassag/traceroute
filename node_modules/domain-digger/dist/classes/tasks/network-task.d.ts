/// <reference types="core-js" />
import Task from './task';
declare class NetworkTask extends Task {
    private connectTimeout;
    private responseTimeout;
    private protocolEncoding;
    private host;
    private port;
    private query;
    private endSign;
    constructor(host: string, port: number, query: string, endSign: string, connectTimeout?: number, responseTimeout?: number, protocolEncoding?: string);
    run(): Promise<string>;
}
export default NetworkTask;
