/// <reference types="core-js" />
import Parser from './parser';
import { IRecord, ITTLPacket } from './i-traceroute-record';
declare class TracerouteParser extends Parser {
    private needToSkipLine;
    private newPacketTTLSource;
    process(): IRecord[];
    protected buildEmptyRecord(): IRecord;
    protected buildPacketTTL(source: string, time: number): ITTLPacket;
    protected stateLineEnter(word: string, records: IRecord[]): Function;
    protected stateHopIndex(word: string, records: IRecord[]): Function;
    protected stateHost(word: string, records: IRecord[]): Function;
    protected stateIpAddress(word: string, records: IRecord[]): Function;
    protected statePacketTTL(word: string, records: IRecord[]): Function;
    protected stateDiffTTLPacketSourceResponseLineEnter(word: string, records: IRecord[]): Function;
}
export default TracerouteParser;
