/// <reference types="core-js" />
import Parser from './parser';
import { IRecord } from './i-lookup-record';
declare class LookupParser extends Parser {
    private linePattern;
    private mxDataPattern;
    private soaDataPattern;
    protected fillObjectUsingPatternDescriptor(parsedData: string[], descriptor: Object): Object;
    protected convertHostAddressToEmail(exchangeAddress: string): string;
    protected prepareSoaDataPatternDescriptor(): Object;
    protected prepareMxDataPatternDescriptor(): Object;
    protected prepareRowTypesDescriptor(): Object;
    protected prepareLinePatternDescriptor(): Object;
    process(): IRecord[];
}
export default LookupParser;
